# 🧩 Simple TypeScript Utilities Library

## 📖 Опис проєкту
Цей проєкт — навчальна TypeScript-бібліотека, створена для закріплення базових понять роботи з **package.json**, залежностями, змінними оточення, семантичним версіонуванням (SemVer) і ключовими можливостями **TypeScript** — типами, інтерфейсами, класами, generics.  
У процесі реалізації налаштовано інструменти перевірки коду (**ESLint**, **Prettier**, **Husky**, **Commitlint**) і автоматизації збірки (**tsup**).  

---

## ⚙️ Встановлення та запуск

```bash
# Клонування репозиторію
git clone <URL_репозиторію>
cd <папка_проєкту>

# Встановлення залежностей
npm install

# Запуск прикладів
npm run demo

# Перевірка коду
npm run typecheck
npm run lint
npm run format:check

# Автоматичне виправлення стилю
npm run lint:fix && npm run format

# Збірка бібліотеки
npm run build
```

---

## 📂 Структура проєкту

```
├─ src/
│  ├─ index.ts       # Головна точка входу бібліотеки
│  ├─ demo.ts        # Приклади використання функцій
│  └─ config.ts      # Завантаження та валідація .env через zod
├─ .env              # Змінні оточення (APP_PRECISION, LOG_LEVEL)
├─ .gitignore
├─ package.json
├─ tsconfig.json
├─ eslint.config.cjs
├─ .prettierrc.cjs
├─ commitlint.config.cjs
└─ .husky/           # Git-хуки pre-commit і commit-msg
```

---

## 🌱 Еволюція версій

| Версія | Зміни | Тип змін |
|:-------:|:------|:---------|
| **0.1.0** | Додано прості функції `add`, `capitalize` з `any` | базова функціональність |
| **0.2.0** | Типізація `add(a:number,b:number)` і `capitalize(s:string)` | minor |
| **0.3.0** | Додано `formatNumber()` із `NumberFormatOptions` | minor |
| **0.4.0** | Додано інтерфейс `User` і generic-функцію `groupBy<T>` | minor |
| **0.5.0** | Додано клас `Logger`, файл `config.ts`, роботу з `.env` і `zod` | minor |
| **1.0.0** | Стабілізація API, заборона `any`, експорт з `package.json` | major |
| **2.0.0** | **Breaking change:** `add()` тепер приймає масив чисел | major |

🔗 Теги релізів:  
`v0.1.0 → v0.2.0 → v0.3.0 → v0.4.0 → v0.5.0 → v1.0.0 → v2.0.0`

---

## 🧠 Приклади використання

```ts
import { add, capitalize, formatNumber, groupBy, Logger, config } from './index';

// Просте додавання
console.log(add([2, 3, 4])); // → 9

// Капіталізація
console.log(capitalize('typescript')); // → 'Typescript'

// Форматування числа
console.log(formatNumber(123.456)); // → '123.456' (з точністю APP_PRECISION)

// Використання generic-функції groupBy
const users = [
  { id: 1, name: 'Alice' },
  { id: 2, name: 'Bob' },
  { id: 3, name: 'Alice' },
];
console.log(groupBy(users, 'name'));
/*
{
  Alice: [{id:1,name:'Alice'}, {id:3,name:'Alice'}],
  Bob:   [{id:2,name:'Bob'}]
}
*/

// Клас Logger зі змінними оточення
const logger = new Logger(config.LOG_LEVEL);
logger.info('App started');
logger.debug('Detailed debug info');
```

---

## 🧾 Файл `.env`

Приклад:
```bash
APP_PRECISION=3
LOG_LEVEL=debug
```

- **APP_PRECISION** — точність форматування чисел (0–10)  
- **LOG_LEVEL** — рівень логування: `silent`, `info`, або `debug`  

`.env` не зберігається у репозиторії (вказано в `.gitignore`).

---

## 🧩 Інструменти розробки

| Інструмент | Призначення |
|:-----------|:-------------|
| **TypeScript** | Типізація, компіляція TS → JS |
| **ESLint** | Аналіз коду, попередження помилок |
| **Prettier** | Єдиний стиль коду |
| **Husky** | Git-хуки для перевірок перед комітами |
| **Commitlint** | Контроль стилю коміт-повідомлень |
| **Zod** | Валідація змінних оточення |
| **tsup** | Збірка бібліотеки у CJS/ESM форматах |

---

## ✅ Перевірки перед комітом
Husky виконує автоматичні перевірки:
```bash
npm run lint && npm run format:check && npm run typecheck
```
Commitlint перевіряє, що повідомлення комітів відповідають формату **Conventional Commits**.

---

## 📦 Збірка та експорти (з версії 1.0.0+)

```json
{
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "require": "./dist/index.cjs",
      "import": "./dist/index.mjs"
    }
  }
}
```

Після збірки команда `npm run build` створює папку `dist/` із трьома файлами:
- CommonJS (`.cjs`)
- ESM (`.mjs`)
- деклараціями типів (`.d.ts`)

---

## 👨‍💻 Автор
**Зотов А.О.**  
*Лабораторно-практична робота №2 — Simple Project*
